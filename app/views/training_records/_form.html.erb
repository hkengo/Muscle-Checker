<%= form_with(model: training_record, local: true) do |form| %>
  <% if training_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training_record.errors.count, "error") %> prohibited this training_record from being saved:</h2>

      <ul>
      <% training_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :video %>
    <%= form.file_field :video, :accept => 'video/*' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
